#!/bin/sh
set -e

if ! command -v mise > /dev/null 2>&1; then
    echo "installing mise"
    brew install mise
fi

if mise install --yes; then
    echo "mise tools installed successfully."
else
    echo "WARNING: some mise tools failed to install."
    echo "You can re-run 'mise install' after fixing issues."
fi
